version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    volumes:
      - ./app:/usr/src/app
      - node_modules:/usr/src/app/node_modules

volumes:
  node_modules: {}
